# Runvoke v0.1.13-dev.1

这是面向 `develop` 分支的预览版本（GitHub Pre-release），用于体验尚未稳定的新功能。不会通过应用内稳定更新渠道推送给正式版用户。

## 新增功能

- 新增默认关闭的本地 MCP 服务：仅监听 `127.0.0.1`，使用 Bearer 令牌认证，可在设置页查看端点、端口和客户端配置。
- MCP 支持查看项目、分组、运行状态和近期日志，编辑已有项目、分组、启动/停止任务、运行临时命令、修改受控设置和请求检查更新；不提供删除能力。
- Agent 可提交候选项目到独立的“Agent 请求纳入项目”弹窗，由用户筛选并确认后才会写入项目配置。
- 支持从 VS Code、Code Insiders、VSCodium 和 Cursor 的本机工作区记录中筛选并导入项目。
- 所有项目导入清单均支持批量或单项使用项目目录名替代探测到的项目名称。

## 问题修复

- MCP 创建分组、编辑项目或移动项目后，前端会同步刷新完整项目与分组列表，避免已归组项目在侧栏隐藏。
- MCP 项目与日志读取不会暴露环境变量值；近期日志会按已保存的环境变量值脱敏。

## 体验优化

- 设置页新增本地 MCP 分区，并适配浅色与深色主题。
- 预发布标签不会覆盖 OSS 的稳定 `latest.json`，避免不稳定版本触发正式用户的应用内更新。
