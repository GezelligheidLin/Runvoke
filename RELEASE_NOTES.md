# Runvoke v0.1.17

## 新增功能

- “打开项目”支持可配置的软件源，默认提供 Visual Studio Code 与 Zed，可在设置中新增、编辑和删除软件源。
- 软件源支持可执行文件、PATH 命令及 Windows `.lnk` 快捷方式，并使用 `{directory}` 参数模板打开当前项目目录。
- 主窗口支持直接拖入一个或多个本地项目文件夹，按目录名添加项目并推断常见启动命令，不会自动运行任务。
- 项目标题区域新增 Git 分支显示，并同步展示暂存文件数和未暂存文件数。
- 新增独立产品官网构建入口与官网内容。

## 问题修复

- 修复 Windows 下自定义软件源启动可能没有反应的问题，改用系统 ShellExecute 能力启动 `.exe` 与 `.lnk`，并返回明确错误。
- 修复外部终端切换 Git 分支后，Runvoke 内分支和工作区状态不能及时更新的问题。
- 修复 Git 状态轮询刷新整个项目列表导致切换项目卡顿的问题。
- 修复项目导入提示会在每次版本更新后重复出现的问题。

## 体验优化

- 软件源参数不经过 shell 执行，Windows 启动时不显示终端窗口。
- Git 状态查询只读取当前选中的项目，避免未选中项目反复启动 Git 子进程和触发列表重绘。
- Git 分支及工作区状态保持为运行时信息，不写入项目配置。
- 设置页的软件源编辑区域采用紧凑折叠布局，未保存草稿删除不会请求后端。
