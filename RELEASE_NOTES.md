# Runvoke v0.1.15

## 新增功能

- 日志工具栏新增本地服务地址定位操作，仅在当前运行实例包含 `http://localhost` 链接时显示，并可一键滚动到主服务地址。

## 问题修复

- 修复同一批日志包含 Vue DevTools、UnoCSS 等多个 localhost 链接时，定位操作可能跳到辅助工具地址的问题；现在优先选择明确标记为 `Local:` 的主服务地址。
- 修复终端区域较窄时标题、筛选项和操作按钮被挤成竖排、遮挡或溢出的问题。

## 体验优化

- 收拢日志筛选与操作区域，窄宽度下将定位、自动滚动、停止和清空切换为紧凑图标，并保持键盘焦点和深浅主题可读性。
- 主窗口默认尺寸调整为宽 1020、高 820，继续保持居中显示及原有最小尺寸约束。
