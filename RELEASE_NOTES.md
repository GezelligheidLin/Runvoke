# Runvoke v0.1.8

本次版本新增项目分组能力，并重点改善高频日志、Webpack 进度输出和任务停止时的稳定性。

## 新增功能

- 支持创建、重命名、删除、展开和收起项目分组；删除分组时项目会安全移至“未分组”。
- 支持项目在组内及跨组拖拽排序，并将分组归属和顺序持久化到本机。
- 项目右键菜单新增分组子菜单，可新建分组或快速移动项目；项目表单支持选择所属分组。
- 自动识别日志中的 HTTP/HTTPS 链接，可在设置中选择使用系统默认浏览器打开或复制到剪贴板。

## 问题修复

- 修复跨分组拖拽后项目节点重复、空分组残留旧项目以及拖拽索引不准确的问题。
- 修复高频日志导致 WebView 卡顿、任务编译速度下降以及停止状态迟迟不刷新的问题。
- 修复 Webpack 进度输出被逐条追加的问题，连续进度现在会在同一条日志中更新。
- 修复任务已回收但界面仍显示运行中，以及停止请求期间缺少明确状态反馈的问题。
- 清理终端 ANSI 控制序列，避免日志中出现转义乱码。

## 体验优化

- Rust 到 WebView 的日志传输改为非阻塞批量分发，前端同步采用批量刷新、浅响应式存储和屏幕外渲染优化。
- 每个运行实例保留最近 2000 条日志，并对异常超长的单条日志进行安全截断，避免长期运行占用过多资源。
- 优化左侧项目列表密度、分组层级、图标对齐和拖拽反馈，保持简洁清晰的视觉结构。
- 分组选择器改为与应用菜单一致的自定义下拉样式，并支持键盘操作和点击外部关闭。
- 左下角在无可用更新时展示当前版本号，便于确认已安装版本。
