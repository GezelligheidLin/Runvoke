# Runvoke v0.1.14

## 新增功能

- 项目侧边栏新增一键展开、收起全部分组的操作，分组内容切换时使用平滑动画，并继续持久化折叠状态。

## 问题修复

- 修复任务结束通知在窗口复用后可能处于其他普通应用浮窗下方的问题。
- 通知每次显示时都会重新提升至 Windows 置顶窗口层级，同时继续保持不抢焦点、不显示任务栏入口。
- 修复项目导入提示会在每次版本更新后重复出现的问题；旧版本用户不再重复弹出，新安装用户最多只在首次升级时提示一次。

## 体验优化

- 点击侧边栏“我的项目”即可展开或收起全部分组，不额外占用操作空间；悬浮说明与其他文字统一使用应用 Tooltip。
