# Runvoke v0.1.7

本次版本重点改善 Windows 窗口缩放体验，并补齐单实例运行能力。

## 新增功能

- 支持单实例运行。再次打开 Runvoke 时会显示、取消最小化并聚焦已有主窗口，不再创建重复窗口或托盘图标。

## 问题修复

- 修复窗口拉伸时根节点主题背景覆盖不完整导致的浅色闪现。
- 修复 WebView2 子窗口边界更新滞后时暴露宿主窗口背景的问题，并在缩放结束后强制完成子窗口重绘。

## 体验优化

- 合并高频 `resize` 事件中的滚动条计算，每个动画帧最多执行一次。
- 仅在滚动条指标实际变化时更新 Vue 状态，减少重复布局与重绘。
- 原生窗口背景与浅色、深色主题保持同步，降低 WebView2 重绘延迟带来的视觉割裂感。
