# Runvoke 项目概览

## 项目目标

Runvoke 是面向本地开发者的桌面项目启动器。它将多个项目的工作目录、启动命令、端口和环境变量集中管理，使用户能在一个应用中启动、停止、重启项目并查看运行日志。

## 当前范围

- 管理项目配置及最多 3 条可保存任务，并将配置持久化在本机。
- 支持拖拽调整项目列表顺序，并在重启应用后保留排序结果。
- 支持创建、重命名、删除及折叠项目分组；项目可通过编辑表单或拖拽调整归属，删除分组时项目自动移到“未分组”。
- 选择项目目录时从常见项目清单识别名称，无法识别时回退为目录名。
- 支持常驻服务、一次任务和不保存的一次性临时命令。
- 同时运行同项目的多个任务；按运行实例查看状态、PID、退出码、stdout、stderr 与系统日志。
- 自动识别日志中的 HTTP/HTTPS 链接，并可在设置中选择使用默认浏览器打开或复制链接。
- 左下角默认显示 GitHub 仓库入口，可在设置中隐藏，并通过系统默认浏览器打开 Runvoke 仓库。
- 项目名称、路径、命令、运行记录及其他发生省略号截断的文本，在悬浮时显示完整内容。
- 可单独或一键移除已停止、已完成或失败的运行记录及其日志，不影响项目配置和活动任务。
- 停止指定运行实例时清理其完整子进程树。
- 支持从项目侧栏停止所有项目的活动任务；全局停止会等待各运行实例及其子进程完成回收。
- 提供系统托盘驻留、开机启动，以及通过 VS Code 或系统文件管理器打开项目目录的能力。
- 仅运行一个应用实例；再次启动 Runvoke 时激活已有主窗口，不创建额外窗口或托盘图标。
- 支持在设置中切换浅色或深色主题，并在本机保留主题偏好。
- 设置使用独立全宽页面，按常规、运行与日志、应用更新分区；进入设置时隐藏侧栏，返回工作台后恢复项目导航。
- 首页和设置页使用统一的可拖拽分栏；两侧均有最小宽度约束，侧栏宽度分别持久化，窗口缩小时自动收敛到可用范围。
- 通过阿里云 OSS 检查已签名的 Windows 更新包，并保留 GitHub Release 作为手动下载安装的备份；应用启动后及每 30 分钟自动检查，并支持在设置中立即检查。安装更新前需二次确认，并先停止所有项目任务，再下载、安装和重启应用。
- 禁用原生右键菜单；在项目卡片上右键可通过自定义菜单直接进入对应项目的编辑弹窗。

不在当前范围内：远程项目管理、团队协作、云端同步或代管项目的构建与部署。

## 技术栈

- 桌面框架：Tauri 2
- 前端：Vue 3、TypeScript、Vite、vue-draggable-plus
- 系统与进程管理：Rust
- 包管理器：pnpm 10
- 发布与更新：GitHub Actions、阿里云 OSS、GitHub Releases、Tauri Updater

## 目录说明

```text
src/                 Vue 前端界面、类型和组合式函数
src-tauri/           Tauri 与 Rust 进程管理实现
.agents/             项目提示词和 AI 协作技能
.agents/skills/runvoke-design-system/ Runvoke 专属界面与交互设计规范
AGENTS.md            项目协作与验证约束
README.md            面向使用者的安装与运行说明
```

## 运行与验证

环境要求：Node.js 20+、pnpm 10+、Rust stable，以及 Tauri 2 在 Windows 上要求的开发环境。

```bash
pnpm install
pnpm tauri dev
pnpm typecheck
pnpm build
cargo check --manifest-path src-tauri/Cargo.toml
```

发布新版本时，使 `package.json`、`src-tauri/tauri.conf.json` 和 `src-tauri/Cargo.toml` 的版本保持一致，更新 `RELEASE_NOTES.md` 中的新增功能、问题修复和体验优化，并推送同版本的 `vX.Y.Z` 标签。GitHub Actions 会构建已签名的 NSIS 安装包，先上传版本化安装包及签名到阿里云 OSS，再上传 `latest.json` 更新清单，同时创建 GitHub Release 备份。仓库 Secret `TAURI_SIGNING_PRIVATE_KEY`、`ALIYUN_OSS_ACCESS_KEY_ID` 和 `ALIYUN_OSS_ACCESS_KEY_SECRET` 必须保存签名私钥和最小权限 OSS 写入凭据。

## 验收标准

- 可新增、编辑、删除并持久化本地项目及其多个任务。
- 可直接拖拽项目调整列表顺序；搜索过滤时也可在可见项目之间排序，插入位置反馈清晰，重启应用后顺序保持不变。
- 分组以轻量一级菜单展示并支持折叠；项目可组内排序或跨组移动，分组删除后组内项目完整保留在“未分组”。
- 从 `package.json`、`Cargo.toml`、`pyproject.toml`、`go.mod`、Maven、`.csproj`、`pubspec.yaml` 和 `composer.json` 中自动填充空的项目名称，且不覆盖手工输入。
- 常驻服务与一次任务可并行执行，运行状态和日志按实例独立查看。
- 临时命令无需保存即可执行；一次任务完成后保留退出结果和日志。
- 停止操作能够回收指定实例的主进程及其子进程。
- 项目侧栏提供停止全部任务入口；操作需二次确认，并在所有活动实例及其子进程回收完成后给出结果。
- 活动任务在预设命令卡片中显示状态，二次点击经确认后可停止；已结束记录可单独或一键经确认后移除，活动实例不可移除。
- 可从“打开项目”下拉选择 VS Code 或系统文件管理器，并打开当前项目目录。
- 同一用户会话中只能存在一个 Runvoke 实例；再次启动时已有主窗口应显示、取消最小化并获得焦点。
- 每个项目最多配置 3 条预设任务；任务区和运行记录位于可滚动工作区，运行记录保持最小可用高度。
- 设置中可切换浅色与深色主题；重启应用后保留用户的主题选择，且不改变项目数据或运行状态。
- 点击侧栏设置入口后显示全宽独立设置页并隐藏侧栏；开机启动、主题、日志链接和更新功能均保持可用，通过“返回工作台”恢复侧栏和项目工作区。
- GitHub 仓库入口默认可见，设置中关闭后立即从左下角隐藏，重启后保持该偏好；点击时使用系统默认浏览器打开固定仓库地址。
- 发生文本截断时才显示 Tooltip，完整内容可通过鼠标悬浮或键盘聚焦读取；Tooltip 具备箭头，并在浅色与深色主题中清晰可读。
- 首页项目侧栏与设置分类侧栏均可拖拽调节宽度；分割线无额外视觉沟槽，拖拽后重启应用仍保留宽度，最小窗口下右侧主要内容不得被挤出。
- 应用启动后及每 30 分钟检查阿里云 OSS 的更新清单；用户可在设置中立即检查。发现新版本后，左下角显示更新标识；点击安装后需通过确认 Popover，停止并等待全部项目任务退出，再验证签名、下载、安装并重启应用。
- 应用内不显示原生右键菜单；项目右键菜单的“编辑”必须打开被右键项目的编辑弹窗。
- 前端类型检查和生产构建通过；修改 Rust 代码时 Cargo 检查通过。

## 变更记录

- 2026-07-28：接入 AIWorkspace，补齐项目概览、标准提示词路径和项目协作技能；未修改应用功能。
- 2026-07-28：升级为多任务工作台，支持常驻服务、一次任务、临时命令和按运行实例管理日志。
- 2026-07-28：新增项目目录清单识别，自动填充项目名称。
- 2026-07-28：项目操作菜单支持使用 VS Code 或系统文件管理器打开当前目录。
- 2026-07-28：预设任务数量限制为 3 条，并将任务与运行记录区域改为内部滚动布局。
- 2026-07-28：支持移除已结束的运行记录和对应日志。
- 2026-07-28：支持一键清除全部已结束的运行记录和日志，并在执行前确认。
- 2026-07-29：日志支持安全识别 Web 链接，并可配置点击后打开或复制。
- 2026-07-28：接入 GitHub Releases 自动发布与签名更新流程，支持应用内检查、下载和安装 Windows 更新包。
- 2026-07-29：禁用原生右键菜单，并为项目卡片新增“编辑”自定义右键菜单。
- 2026-07-29：更新检查调整为每 30 分钟一次，并在设置中提供立即检查入口。
- 2026-07-29：设置新增可持久化的浅色与深色主题切换。
- 2026-07-29：应用改为单实例运行，再次启动时激活已有主窗口。
- 2026-07-29：发布流程使用结构化 Release Notes，并同步到 GitHub Release 与应用更新清单。
- 2026-07-29：项目列表支持拖拽排序，并将顺序持久化到本机配置。
- 2026-07-29：新增可折叠项目分组，支持分组管理、项目归组及跨组拖拽。
- 2026-07-29：将侧栏内设置面板迁移为全宽独立设置页，进入设置时隐藏侧栏，并按常规、运行与日志、应用更新分区。
- 2026-07-29：新增停止全部项目任务入口；安装更新前增加二次确认，并在全部任务与子进程回收完成后继续更新。
- 2026-07-29：首页与设置页接入统一可调整分栏，支持宽度边界、键盘操作和本地持久化。
- 2026-07-29：更新清单与安装包改由阿里云 OSS 分发，标签发布自动上传版本化安装包、签名和更新清单，GitHub Release 保留为手动下载备份。
- 2026-07-30：左下角新增可配置的 GitHub 仓库入口，默认使用系统浏览器打开项目仓库。
- 2026-07-30：接入 shadcn-vue 与 Reka UI Tooltip，为截断文本提供完整内容提示。
