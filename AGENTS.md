# Runvoke 协作规范

## 项目目标

本项目是一个基于 Tauri 2、Vue 3 和 TypeScript 的桌面项目启动器，用于集中配置、启动、停止和查看本地开发项目的运行日志。

## 技术约束

- 包管理器使用 pnpm。
- 前端使用 Vue 3 Composition API 与 `<script setup lang="ts">`。
- 桌面端能力通过 Tauri 2 和 Rust 实现。
- 不随意更换现有技术栈、目录结构或状态管理方案。
- 进程启动必须隐藏终端窗口；停止指定运行实例时应清理其完整子进程树。
- 停止全部任务必须复用运行实例的停止路径，并等待所有活动实例及其子进程完成回收；部分失败或超时不得报告成功。
- 项目任务分为常驻服务和一次任务；进程、状态及日志必须以运行实例 `runId` 隔离，不得让同项目任务互相覆盖。
- 选择本地项目目录后，可读取常见语言与框架的项目清单推断名称；自动识别不得覆盖用户已输入的名称。
- 不在日志或配置中泄露密钥、令牌等敏感信息。
- 日志中的 HTTP/HTTPS 链接可按本地设置使用默认浏览器打开或复制；外部打开前必须校验协议，禁止执行任意命令或非 Web 协议。
- 左下角 GitHub 仓库入口默认显示，可在设置中隐藏；点击时仅可通过既有安全外链能力打开本项目的固定仓库地址。
- 所有使用省略号截断的展示文本必须提供基于 shadcn-vue 与 Reka UI 的 Tooltip；仅在实际溢出时显示完整内容，并保持深浅主题可读性。
- 应用更新使用 Tauri 官方更新器与阿里云 OSS 分发，GitHub Release 保留为手动下载备份；应用启动后及每 30 分钟自动检查稳定更新，并在设置中允许用户立即检查。用户可主动开启接收预览版本，预览更新从独立清单读取并必须明确提示其可能不稳定；安装前必须二次确认并先停止全部项目任务，停止失败或超时不得继续更新。更新包必须签名，私钥与 OSS 写入凭据只允许保存在本机安全位置或 GitHub Secret，禁止写入仓库。
- 应用提供浅色与深色主题切换；主题选择应持久化在本机，且不影响项目配置、运行状态或日志。
- 应用设置使用独立全宽页面承载；设置入口保留在左侧边栏，进入设置后隐藏边栏，返回工作台时恢复，不在边栏内展开配置表单。
- 应用只允许一个主实例；再次启动时不得创建新窗口或托盘图标，而应激活已有主窗口。
- 项目列表支持拖拽调整顺序；新顺序必须持久化到本机项目配置，重启后保持不变。
- 项目列表排序使用 `vue-draggable-plus`，并启用 fallback 拖拽以保证 Windows WebView2 中的交互稳定性。
- 项目支持本地持久化分组；删除分组只能解除项目归属，不得删除组内项目。项目应支持组内及跨组拖拽。
- 禁用应用内原生右键菜单；项目卡片的自定义右键菜单应使用明确、低风险的项目操作。
- 首页与设置页的侧栏宽度调整统一使用 `ResizableSplitPane.vue`；侧栏和内容区必须有最小宽度约束，用户宽度选择应本地持久化，设置页切换不得丢失首页侧栏状态。
- `main` 仅用于稳定正式版本；`develop` 是允许未完成新功能的预览集成分支，后续独立功能从 `develop` 创建 `feature/<功能名>` 分支。`develop` 不得推送正式发布标签，但可使用带预发布标识的 SemVer 标签（如 `v0.1.13-dev.1`）创建 GitHub Pre-release；预发布必须写入独立的 OSS `latest-prerelease.json`，不得覆盖稳定 `latest.json`，且仅对主动开启预览更新的用户可见。
- 设置中的项目配置目录入口必须由 Rust 固定定位应用配置目录后打开系统文件管理器，不得接受或执行任意前端路径。
- 项目导入支持 VS Code 与 Cursor；只能读取 Code、Code Insiders、VSCodium 或 Cursor 的用户级工作区记录，导入前必须展示可勾选清单、按规范化路径去重，且不得自动执行导入项目的命令。
- 本地 MCP 服务默认关闭，仅监听 `127.0.0.1`，所有请求必须使用本机保存的 Bearer 令牌；令牌与环境变量值不得出现在日志、Toast、错误信息或仓库中。
- MCP 只提供查看、启动、停止、编辑、分组、受控设置与更新请求等工具，不得暴露任何删除项目、分组或运行记录的工具。MCP 项目导入只能接收 Agent 显式提交的候选目录并打开独立的 Agent 前台可勾选清单，不得读取或混入 VS Code/Cursor 记录，也不得复用普通项目导入流程；由用户确认后才可写入。更新安装同样必须在前台确认。
- MCP 修改项目或分组后必须通知前端刷新完整工作区数据；不得只刷新项目或分组之一，以免已归组项目在侧栏被错误隐藏。

## 编码规范

- 优先使用清晰的小组件、组合式函数和显式 TypeScript 类型。
- 设计、重构或审查页面与组件样式时，先阅读并遵循 `.agents/skills/runvoke-design-system/SKILL.md` 及其设计规范参考。
- 界面文案默认使用简体中文。
- 新增任务功能应覆盖常驻任务重复启动、一次任务并发、临时命令、异常退出和指定实例停止等边界情况。
- 修改 Rust 进程管理逻辑后，应验证 Windows 下的隐藏窗口和进程回收行为。

## 验证要求

- 前端改动至少执行 `pnpm typecheck` 和 `pnpm build`。
- Rust 改动至少执行 `cargo check --manifest-path src-tauri/Cargo.toml`。
- 修改更新器或发布工作流时，验证 Tauri 配置、更新器权限、GitHub Actions YAML、签名环境变量名称、OSS 变量与 Secret 名称，以及安装包和签名先于稳定 `latest.json` 或预览 `latest-prerelease.json` 上传的顺序；确认预览检查默认关闭且安装提示包含不稳定警告。
- 修改本地 MCP 时，验证未授权请求返回 401、工具清单不含删除工具、导入请求只打开前台清单且不自动写入、令牌不进入日志，并确认服务关闭后端口不再监听。
- 发布版本时同步更新 `RELEASE_NOTES.md`，并明确说明新增功能、问题修复和体验优化。
- 提交前说明已完成的验证以及尚未验证的内容。

## 文档同步

- 修改项目目标、功能范围、技术栈、目录结构、运行方式或验收标准时，必须同步更新 `PROJECT_OVERVIEW.md`、`.agents/PROJECT_PROMPT.md` 与 `.agents/skills/runvoke/SKILL.md`。
- 仅修改实现细节且不影响上述内容时，无需更新项目文档，但应在交付说明中注明原因。
